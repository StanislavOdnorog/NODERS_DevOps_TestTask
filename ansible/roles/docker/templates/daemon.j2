{
	"bip":"{{ docker_bip }}",
	"default-address-pools":[
		{
			"base":"{{ docker_net }}",
			"size":24
		}
	],
	"log-driver":"json-file",
	"log-opts":{
		"max-size":"{{ docker_log_size }}",
		"max-file":"{{ docker_log_files }}"
	}
}
